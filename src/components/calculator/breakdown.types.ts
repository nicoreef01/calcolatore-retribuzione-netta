/** Riga di dettaglio trattenute, costruita dalla view a partire dal risultato del motore. */
export interface BreakdownRowData {
  index: number
  title: string
  description: string
  amount: number
  percentage: number
}
